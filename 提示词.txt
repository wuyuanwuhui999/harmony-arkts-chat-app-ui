# 角色
你是一个资深的 harmony arkts 鸿蒙原生开发专家，严格遵守以下规范，为我的 App 实现功能。

# 核心任务
阅读 `txt` 文档里面的ets源代码，根据以下要求，生成符合规范的代码：
- UserPage页面切换公司按钮下方添加一个用户管理按钮，样式跟修改密码样式相同，退出登陆按钮到最下面，根据CompanyPage中选择的company对象中的role字段判断当前登录人是系统管理员或超级管理员，当前登录人是系统管理员或超级管理员才能显示用户按钮，点击添加用户管理，跳转到UserManagePage页面，Company模型增加一个positionId字段，根据company.role判断角色，普通管理员role=1，超级管理员role=2，判断role大于0即可显示用户管理按钮
- positionId是职务字段，字符串类型
- UserManagePage.kt页面没有开发，帮我新建一个UserManagePage页面，UserManagePage用户管理页面与TenantManagePage页面样式相同
- UserManagePage.kt页面标题栏显示标题名称为：用户管理，标题栏右边显示一个加号图标，内容区背景是灰色，设置内容区标准内边距，内容区有一个胶囊型搜索框，搜索框下方是用户列表，用户列表是显示该公司下的用户，调用接口获取该公司下的用户，接口地址是：/service/company/getCompanyUsers?companyId={companyId}&pageSize={pageSize}&pageNum={pageNum}&keyword={keyword}，返回的data是User对象数组，支持往上滚动分页加载，用户列表显示用户头像和用户姓名，用户姓名下面显示用户工号，用户工号显示灰色，右边如果是管理员，显示管理员小标签
- 用户列表做成抽屉式，向左滑动用户记录显示设为管理员/取消管理员按钮，如果该用户是管理员而且当前登录人是超级管理员，才能显示取消管理员；如果该用户不是管理员而且当前登录人是超级管理员，才能显示设为管理员；当前登录人是普通管理员，不要显示设为管理员/取消管理员按钮
- 设为管理员/取消管理员按钮右边是一个红色的删除按钮，当前登录人是普通管理员和超级管理员都可以显示删除按钮，点击删除按钮，弹出警告框提示是否删除用户；点击确定之后，调用服务接口：/service/company/removeUser/{userId}/{companyId}，请求方式是delete，返回的data大于0表示删除成功，弹出提示删除用户成功，失败弹出返回结果的msg提示
- getCompanyUsers接口keyword接口可以为空，点击搜素按钮，调用getCompanyUsers查询当前公司内的用户，显示在用户列表中
- 点击标题栏右边的加号图标，跳转到AddUserPage页面，AddUserPage页面没有开发，AddUserPage页面跟AddTenantUserPage页面相似，标题栏显示添加用户，内容区背景是灰色，内容区里面有一个胶囊型搜索框，搜索框下面是一个搜索结果用户列表
- 在AddUserPage里面输入关键字搜索时，调用接口：/service/user/searchUsers?companyId={companyId}&pageNum={pageNum}&pageSize={pageSize}，支持往上滚动分页加载，返回的data是一个User对象用户列表，但比User对象中多了一个checked字段，可以新建一个模型继承User模型处理搜索接口返回的数据，checked：0表示该用户不在该公司中，搜索结果用户列表中显示激活的添加按钮，checked=1表示该用户已经在该公司中，搜索结果用户列表中显示灰色的已添加按钮
- 用户列表中每一条记录用一条横线隔开
- 点击搜索结果中的添加按钮，弹出对话框，对话框中如果当前用户是超级管理员，显示角色单选项：普通用户/管理员，默认选中普通用户，普通管理员不能设置role角色，普通管理员只能设置普通用户，只有超级管理员才显示角色选项
- 角色下面显示一个部门选择框，调用接口获取所有部门，接口地址如下/service/company/getDepartments?companyId={companyId},返回的data是部门列表，部门结构如下
{id,companyId,departmentName,description,createTime}
- 部门选择框下面是职位选择框，选择了部门之后，调用后端接口获取部门下面的所有职位，获取职位接口：/service/company/getPositions?departmentId={departmentId}，返回的data是职位列表，职位结构如下
{id,positionName,departmentId,description,createTime}
- 选择了部门和职位之后点击对话框中的确定按钮，调用接口：调用接口：/service/company/addUser，请求方式是post,请求参数如下
{userId,companyId,role,positionId}，role:1管理员，role=0普通用户，role只能设置0和1，当前登录人是普通管理员不能设置role=1，超级管理员才能设置role=2
- 如果当前用户是普通管理员role=1，添加添加按钮，不要弹出选项，直接调用addUser接口保存用户
- 返回结果的data>0表示添加成功，添加按钮变成灰色，按钮文字变成已添加

# 开发规范（必须严格遵守）

## 1. 文件组织结构
- 完整页面放在 `page/`
- 可复用组件放在 `components/`
- 所有数据类型放在 `type/index.ets` 文件下。
- 所有工具方法放在 `utils/` 目录下
- 所有api地址放在 `api/index.ts`文件下
- 请求后端的方法放在 `service/index.ets` 文件下

## 2. 间距与尺寸规范
- **所有间距**（padding、margin、图标间距、按钮间距、图文间距、布局间距、边距、条目上下间隔等）统一使用 `theme/size.middleGap`。
- 按钮高度：`theme/size.btnHeight`，圆角 = `size.btnHeight / 2`。
- 输入框/搜索框高度：`theme/size.inputHeight`，圆角 = `size.inputHeight / 2`。
- 卡片圆角：`theme/size.middleBorderRadius`。
- 图标尺寸：
  - 小图标：`theme/size.smallIcon`
  - 中图标：`theme/size.middleIcon`
  - 大图标：`theme/size.bigIcon`
  - 图标透明度：`theme/size.opacity`
- 字体大小：
  - 正常：`theme/size.middleFont`
  - 主标题：`theme/size.bigFont`

## 3. 颜色与主题规范
- 页面背景色：`theme/color.pageBackground`
- 应用主色调：`theme/color.primary`
- 按钮颜色：
  - 确定/激活按钮背景：`color.primary`，文字白色 `color.white`
  - 取消按钮背景：透明，边框和文字颜色：`color.gray`
  - 禁用按钮背景：`color.gray`
  - 删除按钮背景：`color.warn`

## 4. 数据与接口规范
- 所有后端接口返回格式统一为 `type/MyAwesomeData<T>`，其中：
  - `data`: 任意类型（JSON、数组、字符串、数字）
  - `total`: 分页接口返回总条数，非分页接口为 `null`
  - `msg`: 失败时的服务器信息
  - `status`: `"SUCCESS"` 表示成功，其他表示失败
- 请求方法
  - 所有请求后端方法都放到 `service/inde.ets`中，使用通用网络请求对象httpRequest进行网络请求

# 代码输出要求
1. **文件大小限制**：
   - 若文件代码 ≤ 500 行，输出完整文件。
   - 若文件代码 > 500 行，只输出修改或新增的完整方法体。
2. 修改代码后，请明确说明：**修改了哪个文件**、**修改了哪个方法/类**。
3. 为每个公开方法及核心逻辑添加**注释**。
4. 请先确认理解以上全部规范，再开始生成代码。

# 开始
请根据上述规范，实现【核心任务】中描述的功能。